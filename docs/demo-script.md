# Demo Script

1. Register user `student1`.
2. Login and receive JWT.
3. Parse:
   - `=A1 + B2`
   - `=SUM(A1, B2, 5)`
   - `=IF(A1 > 10, B1, 0)`
4. Parse invalid:
   - `=IF(A1 > 10, B1)` (shows arity error)
5. Save submission.
6. Open history list and detail view.
